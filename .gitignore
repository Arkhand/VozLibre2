# Dependencias
node_modules/

# Builds / empaquetado
dist/
dist2/
out/
build/

# Logs
npm-debug.log*
*.log

# Caches y temporales
.cache/
*.tmp

# SO
Thumbs.db
.DS_Store

# Datos locales de Electron (perfiles, userData de desarrollo)
*.local

# Configuración del usuario (contiene la API key) — NUNCA al repo
settings.json

# Locks/temporales de Claude
.claude/scheduled_tasks.lock

# Audios/videos de prueba sueltos en la raíz (pesados, no son parte del proyecto).
# El prefijo "/" limita el patrón a la raíz: si algún día hay audio en assets/,
# ese sí entra al repo.
/*.mp4
/*.mkv
/*.mov
/*.ogg
/*.opus
/*.m4a
/*.mp3
/*.wav
